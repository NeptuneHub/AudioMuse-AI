onnxruntime==1.19.2
# CPU image is torch-free as of v4: e5 / silero / Marian / whisper all run via
# raw onnxruntime against pre-exported ONNX models. The build-time exports
# (which DO require torch) live in the standalone ``onnx-exporter`` Dockerfile
# stage and never reach the runtime image. See requirements/gpu.txt for the
# torch lines used by GPU builds.
