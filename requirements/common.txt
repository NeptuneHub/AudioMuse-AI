numpy==1.26.4
scipy==1.16.3
numba==0.60.0
soundfile==0.13.1
Flask==3.1.3
Flask-Cors==6.0.2
redis==7.4.0
requests==2.33.1
scikit-learn==1.8.0
rq==2.7.0
pyyaml==6.0.3
six==1.17.0
rapidfuzz==3.14.5
psycopg2-binary==2.9.12
ftfy==6.3.1
flasgger==0.9.7.1
sqlglot==30.6.0
google-genai==1.57.0
mistralai==1.12.4
umap-learn==0.5.12
# Use pozalabs fork to avoid Python 3.12 SyntaxWarning until upstream releases a proper fix (see: https://github.com/jiaaro/pydub/issues/801)
pozalabs-pydub==0.37.0
python-mpd2==3.1.1
psutil==7.2.2
onnx==1.20.0
resampy==0.4.3
librosa==0.11.0
flatbuffers==25.12.19
packaging==26.2
protobuf==6.33.6
sympy==1.13.1
mcp==1.27.0
httpx==0.28.1
voyager==2.1.0
# Transformers must satisfy optimum 1.27.0's upper bound (<4.54.0). 4.53.3 is
# the version used by scripts/onnx_export/run_exports.sh when producing the
# ONNX bundles, so runtime + export are kept identical.
transformers==4.53.3
huggingface-hub==0.36.2
sentencepiece==0.2.1
# Whisper transcription + Marian translation run via HuggingFace optimum's
# ORTModel* classes against the pre-exported ONNX bundles in /app/model/.
# This replaced ~600 lines of hand-rolled greedy-decode loops that broke on
# non-trivial source languages (e.g. Italian Marian translation produced
# empty output because we never fed past_key_values to the decoder).
# e5 + silero VAD still use raw onnxruntime — they are single-pass models
# that don't need beam search or KV-caching.
# Legacy ``openai-whisper`` / ``silero-vad`` / ``llama-cpp-python`` deps are
# all gone (STEP 5 Qwen LLM cleanup is also removed — raw whisper output
# is good enough for axis scoring).
# Pin must match the version in scripts/onnx_export/run_exports.sh that
# produced the ONNX bundle, so the runtime sees the exact ONNX layout the
# exporter wrote (especially for decoder_model_merged.onnx which evolved
# between optimum minor versions).
optimum[onnxruntime]==1.27.0
langdetect==1.0.9
stopwords==1.0.2
PyJWT==2.12.1
argon2-cffi==25.1.0
gunicorn==25.3.0
zstandard==0.25.0